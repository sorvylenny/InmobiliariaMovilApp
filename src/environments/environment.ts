// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  baseUrl:'https://inmobiliaria-flores-m-api.vercel.app/inmuebles/',
  baseUrlUser:'https://inmobiliaria-flores-m-api.vercel.app/users/',
  baseUrlPais: 'https://api-colombia.com/api/v1/',
  mapBoxToken: 'pk.eyJ1Ijoic29ydnlsZW5ueTIyIiwiYSI6ImNsdWo4YzMwMDBjaG8yaWxoN2lwbGVzeHIifQ.yaw8Nyo3b5Rw_o8AGB9_lg'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
